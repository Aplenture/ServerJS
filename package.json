{
    "name": "serverjs",
    "version": "0.1.0",
    "description": "",
    "main": "dist/index.js",
    "scripts": {
        "build": "rm -rf dist; mkdir dist; tsc -p tsconfig.json; npm test",
        "test": "mocha -r ts-node/register 'tests/**/*.ts' --timeout 6000",
        "postinstall": "npm run build"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/Aplenture/ServerJS.git"
    },
    "author": "Aplenture",
    "license": "ISC",
    "bugs": {
        "url": "https://github.com/Aplenture/ServerJS/issues"
    },
    "homepage": "https://github.com/Aplenture/ServerJS#readme",
    "dependencies": {
        "foundationjs": "github:Aplenture/FoundationJS",
        "mariadb": "^3.0.2"
    }
}
